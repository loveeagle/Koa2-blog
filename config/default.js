const config = {
  // 启动端口
  port: 1337,
  // 数据库配置
  database: {
    DATABASE: 'nodesql',
    USERNAME: 'root',
    PASSWORD: 'sy559649',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config